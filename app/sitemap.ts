import type { MetadataRoute } from "next";
export default function sitemap(): MetadataRoute.Sitemap { const base = process.env.NEXT_PUBLIC_CANONICAL_URL || "https://lomnickpro.com"; return ["", "/about", "/engineering", "/lomnickpro", "/community-leadership", "/lionheart", "/lionheart/volume-1-preview", "/lionheart/volume-2-preview", "/contact"].map((path) => ({ url: `${base}${path}`, lastModified: new Date(), changeFrequency: path === "" ? "monthly" : "yearly", priority: path === "" ? 1 : 0.7 })); }
